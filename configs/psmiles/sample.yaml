# PolySmith sampling (delegated to unified_cli)
version: v4
mode: tg
output_path: outputs/psmiles/samples.csv
extra_args:
  - --checkpoint
  - models/PolySmith/checkpoints/finetune_tg_modelv4.pt
  - --target-tg
  - "450"
  - --num-per-target
  - "64"
