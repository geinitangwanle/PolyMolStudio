# Tests

Place unit tests for shared helpers and module entrypoints here, e.g.:
- `test_chem_utils.py` for RDKit validity/canonicalization helpers.
- `test_psmiles_tokenizer.py` for tokenizer encoding/decoding.
- `test_gnn_dataset.py` for manifest/DataLoader wiring.

These are placeholders; add real tests as implementations stabilize.
